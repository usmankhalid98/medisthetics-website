/* medisthetics → Supabase connection.
   1. Create a free project at https://supabase.com/dashboard
   2. Paste your Project URL + anon public key below, commit & push.
   The anon key is SAFE to commit: database rules (schema.sql) let the
   public only READ products; writing needs an admin login. */
window.MEDISUPABASE = {
  url: '',
  anonKey: ''
};
